const defaultState = {
    inputValue : '123',
    list: [
        'I love u ',
        'u bad bad',
        'I like like'
    ]
}

export default (state = defaultState, action) => {
    //state 整个仓库存储数据
    return state;
}